/**
 * Fonts for in-scene (troika) text. These are self-hosted single-subset
 * .woff files (troika does not read .woff2) vendored in public/fonts with
 * their OFL licenses; the DOM uses next/font instead.
 */
export const FONT_MONO_3D = "/fonts/ibm-plex-mono-latin-500-normal.woff";
export const FONT_SANS_3D = "/fonts/inter-latin-600-normal.woff";
